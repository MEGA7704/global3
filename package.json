{
  "name": "global3-cloudflare-pages",
  "version": "1.0.0",
  "private": true,
  "description": "GLOBAL 3 prêt pour GitHub et Cloudflare Pages avec API Functions KV/D1.",
  "scripts": {
    "build": "node -e \"console.log('Projet statique : dossier de sortie public/')\"",
    "check": "node --check public/assets/app.js && node --check functions/api/load.js && node --check functions/api/save.js && node --check functions/api/session.js && node --check functions/api/health.js",
    "dev": "wrangler pages dev public --functions functions",
    "deploy": "wrangler pages deploy public --project-name global3-cloud-backup"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
